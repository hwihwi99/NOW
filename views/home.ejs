<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./css/nav.css">
    <link rel="stylesheet" href="./css/home.css">
    <link rel="stylesheet" href="./css/table.css">
</head>
<body>
    <div class="home">
        <nav>
            <a href="/home">
                <img src="./img/now_logo.png" alt="로고 사진 입니다.">
            </a>
            <div class="menu">
                <p class="nav-intro">
                    <a href="/profile"><%=nickname%>님</a> 반갑습니다. 이곳은 홈화면입니다.
                </p>
                <div class="text_menu">
                    <a href="/board"><h1>게시판 구경가기</h1></a>
                </div>
            </div>
        </nav>  
    <hr>
    <br>
    <table>
        <tr>
            <th class="table-header">즐겨찾기 목록</th>
        </tr>
        <%if(bookmark.length>0) {%>
            <tr>
                <td>
                    <%for(var i = 0; i < bookmark.length; i++) {%>
                        <form action="/board/boardname=<%= bookmark[i].id%>" method="post">
                            <input style="padding-top: 5px; padding-bottom: 5px;" type="submit" name="board_title" id="board_title" class="title" value="<%= bookmark[i].title %>">
                        </form>      
                        <hr class="line">
                    <% } %>  
                </td>              
            </tr>       
        <%} else {%>
            <td>
                즐겨찾기한 게시판이 없습니다!
            </td>        
        <%}%>
    </table>

    <br>

        
    <table>
        <tr>
            <th class="table-header">최근 업로드 게시판</th>
        </tr>
        <%if(list.length>=10) {%>        
            <tr>
                <td>
                    <%for(var i = 0; i < list.length; i++) {%>
                        
                        <form action="/post/<%= list[i].id%>" method="post">
                            <input type="submit" name="post_title" id="post_title"  class="title" value="<%= list[i].title %>">
                        </form>        
                        <br>
                        <p class="sub-content">
                            <%=list[i].nickname%> | <%=list[i].board_title%>
                        </p>
                        <hr>
                    <% } %>                    
                </td>                
            </tr>    
        <%} else if (list.length>0) {%>
            <tr>
                <td>
                    <%for(var i = 0; i < list.length; i++) {%>
                        <form action="/post/<%= list[i].id%>" method="post">
                            <input type="submit" name="post_title" id="post_title"  class="title" value="<%= list[i].title %>">
                        </form>        
                        <br>
                        <p class="sub-content">
                            <%=list[i].nickname%> | <%=list[i].board_title%>
                        </p>
                        <hr>
                    <% } %>                    
                </td> 
            </tr>    
        <%}else {%>
            <tr>
                등록된 게시글이 없습니다.
            </tr>
        <%}%>
    </table>
    <br>
    <table >
        <tr>
            <th class="table-header">인기 업로드</th>
        </tr>
        <%if(list.length>=5) {%>        
            <tr>
                <td>
                    <%for(var i = 0; i < 5; i++) {%>
                        
                        <form action="/post/<%= like[i].id%>" method="post">
                            <input type="submit" name="post_title" id="post_title"  class="title" value="<%= like[i].title %>">
                        </form>        
                        <br>
                        <p class="sub-content">
                            <%=list[i].nickname%> | <%=list[i].board_title%>
                        </p>
                        <hr>
                    <% } %>                    
                </td>                
            </tr>    
        <%} else if (like.length>0) {%>
            <tr>
                <td>
                    <%for(var i = 0; i < like.length; i++) {%>
                        <form action="/post/<%= like[i].id%>" method="post">
                            <input type="submit" name="post_title" id="post_title"  class="title" value="<%= like[i].title %>">
                        </form>        
                        <br>
                        <p class="sub-content">
                            <%=list[i].nickname%> | <%=list[i].board_title%>
                        </p>
                        <hr>
                    <% } %>                    
                </td> 
            </tr>    
        <%}else {%>
            <tr>
                좋아요를 받은 게시판이 없습니다.
            </tr>
        <%}%>
    </table>
        
    </div>
</body>
</html>